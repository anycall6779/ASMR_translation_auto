# ASMR Studio — Termux 의존성
#
# ⚠ 직접 pip install -r 하지 말고 setup.sh 사용 권장
# scipy/numpy는 pkg로 먼저 설치해야 컴파일 없이 빠름:
#   pkg install python-numpy python-scipy
#   pip install -r requirements.txt --prefer-binary

# 웹 서버
flask>=3.0.0

# 번역 (Playwright 대신 — Termux 환경)
deep-translator>=1.11.0

# 오디오 처리 (numpy/scipy는 pkg로 설치)
soundfile>=0.12.1
noisereduce>=3.0.2

# STT (둘 중 하나 설치)
faster-whisper>=1.0.0
# openai-whisper>=20231117  # faster-whisper 실패 시
